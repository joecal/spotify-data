{
  "name": "spotify-data-server",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' --files src/index.ts",
    "build": "rm -rf dist && tsc -p ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.1.1",
    "typescript": "^4.1.2"
  }
}
