import Artist, { IArtist } from "./artist.model";

export default class ArtistsDAL {
  //   async createArtist(): Promise<IArtist> {
  //     return Artist.create({
  //     })
  //       .then((artist: IArtist) => artist)
  //       .catch((error: Error) => {
  //         throw error;
  //       });
  //   }
}
